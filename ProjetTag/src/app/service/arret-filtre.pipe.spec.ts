import { ArretFiltrePipe } from './arret-filtre.pipe';

describe('ArretFiltrePipe', () => {
  it('create an instance', () => {
    const pipe = new ArretFiltrePipe();
    expect(pipe).toBeTruthy();
  });
});
